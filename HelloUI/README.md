
### UI
 This is a Renderer for UI. The Kerning and Rasterization is handled by the freetype library. I export the glyphs to a glyph atlas, and create 2D planes for the text
fields and do math to place the glyphs on the planes during rendering.


```mermaid
    graph TD;
```

